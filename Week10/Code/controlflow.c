#include <stdio.h>

// Structure of if statements in C

int main (void)
{
    if (/* condition 1 */) {
        // Statements
    }
    else if (/* condition 2 */) {
        // Statements
    }
    else if (/* condition 3 */) {
        // Statements
    }

    return 0;
}