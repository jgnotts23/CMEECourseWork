#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main (void)
{
    //int num = 6;
    printf("%f\n", pow(2, 3)); // raise a to the power of b in (a, b)
    printf("%f\n", sqrt(36)); // square root
    printf("%f\n", ceil(36.356)); // round up
    printf("%f\n", floor(36.356)); // round down

    return 0;
}