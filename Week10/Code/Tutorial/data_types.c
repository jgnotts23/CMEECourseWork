#include <stdio.h>
#include <stdlib.h>

int main (void)
{
    int age = 40;
    double gpa = 3.7;
    char grade = 'A';
    char phrase[] = "Giraffe"; //technically an array of characters rather than a datatype

    return 0;
}