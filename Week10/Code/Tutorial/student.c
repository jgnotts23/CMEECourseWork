#include <stdio.h>
#include <stdlib.h>

int main (void)
{
    int hoursStudied = 60;
    int kidsBeatUp = 0;

    if ((hoursStudied >= 10) && (kidsBeatUp == 0)) {
        printf("You are a good student\n");
    }

    return 0;
}